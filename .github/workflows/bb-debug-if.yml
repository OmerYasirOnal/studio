name: bb-debug-if

on:
  push:
    branches: [debug/bb-workflows]
    paths:
      - '.github/workflows/bb-debug-if.yml'

jobs:
  hello:
    runs-on: ubuntu-latest
    if: ${{ hashFiles('games/brave-bunny/app/package.json') != '' }}
    steps:
      - run: echo "if-guard works"
