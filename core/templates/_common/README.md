# `_common/` template

Files in this directory are copied into **every** game's directory in addition to the genre-specific template. Used for things every game needs (gitignore additions, license ledger header, etc.) regardless of genre.
