// Stub — replaced by Stream S8 (Task 38: lifecycle / pickup magnet).
// Kept minimal so S6's runLoop typechecks before S8 lands.
export function tickLifecycle(_delta: number): void {
  /* S8 */
}

export function setMagnetRadius(_r: number): void {
  /* S8 */
}
