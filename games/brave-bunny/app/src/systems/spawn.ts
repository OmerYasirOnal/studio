// Stub — replaced by Stream S7 (Task 37: wave spawner).
// Kept minimal so S6's runLoop typechecks before S7 lands.
export function tickSpawn(_delta: number): void {
  /* S7 */
}

export function resetSpawn(): void {
  /* S7 */
}
