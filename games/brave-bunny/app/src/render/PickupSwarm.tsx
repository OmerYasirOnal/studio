// Stub — replaced by Stream S7/S8 (pickup rendering + magnet behaviour).
// Kept minimal so S6's Game.tsx typechecks + renders before S7 lands.
export default function PickupSwarm() {
  return null;
}
