# crash-fix-engineer — 2026-05-13 11:12 (Europe/Istanbul)

## Mission
Close GH issue #1 — TestFlight 0.1.0(2) crashes on launch because Boot/MainMenu/Loadout/PerfStress shipped as empty 12-YAML-doc placeholder scenes.

## Changes
1. **`Assets/Editor/SceneSetup.cs`** — added `ForceScaffold()` MenuItem + `RunHeadlessForce()` CLI entry; deletes pre-existing Boot/MainMenu/Loadout/PerfStress `.unity`+`.meta` first, then runs the existing EnsureX flow (Run.unity preserved). `EnsureBoot` now adds MainCamera+AudioListener (bunny-cream clear) and `[SceneFlow]` GameObject sibling to `[Bootstrap]`. `EnsurePerfStress` now attaches `Brave.Diagnostics.FpsSampler` to the MainCamera (fixes PerfStressFpsTest dependency). `RegisterInBuildSettings` rewrites canonical entries (so stale all-zero GUIDs are replaced on every scaffold).
2. **`Assets/_Brave/Code/Systems/Context/SceneFlow.cs` (NEW)** — MonoBehaviour with `DontDestroyOnLoad`; on `Awake` subscribes to `Bootstrapper.GameContextReady` → `SceneManager.LoadSceneAsync(nextScene, Single)`. Default `nextScene="Run"`. Handles `IsReady`-already-true race; idempotent unsubscribe; null/empty `nextScene` guard.
3. **`Assets/_Brave/Code/Systems/Context/GameContextBootstrap.cs`** — null-safe `_mixer` (skip-mode + warning) and `_languageTables` (empty array + warning) so missing SerializeFields no longer NRE in iOS player.
4. **`ProjectSettings/EditorBuildSettings.asset`** — regenerated by scaffold: MainMenu/Loadout/PerfStress GUIDs now 16-hex non-zero.
5. **Pre-existing compile errors fixed** (blocked any Unity batchmode run):
   - `VirtualJoystickInput.cs` — `using TouchPhase = UnityEngine.InputSystem.TouchPhase` (ambiguous-ref).
   - `iOSAppIconAssigner.cs` — `BuildTargetGroup.iOS` instead of `NamedBuildTarget.iOS` for legacy `GetSupportedIconKindsForPlatform`.
   - `EnemyRegistryTests.cs` — `Has.No.Member(obj)` instead of `Does.Not.Contain(obj)` (NUnit string-overload).
   - New `Assets/_Brave/Code/Diagnostics/Brave.Diagnostics.asmdef` + added to `Brave.Tests.PlayMode.asmdef` references.

## Scene line counts — before / after
| Scene        | Before | After | YAML docs after |
|--------------|-------:|------:|----------------:|
| Boot.unity   |    235 |   374 |              19 |
| MainMenu     |    266 |   266 |              12 |
| Loadout      |    266 |   266 |              12 |
| PerfStress   |    316 |   329 |               — |
| Run          |    773 |   773 |       unchanged |

Boot now contains: MainCamera (+ AudioListener), `[SceneFlow]` (+ SceneFlow MB), `[Bootstrap]` (+ GameContextBootstrap MB), EventSystem.

## Tests — Unity 6000.0.74f1
| Suite      | Total | Pass | Fail | Note |
|------------|------:|-----:|-----:|------|
| EditMode   |   209 |  198 |   11 | 11 failures are pre-existing XZ-migration debt (EnemyRegistry NRE, AutoAttack acquisition, ProjectileMath decay, SaveService bak2 fallthrough). NOT regressions from this change. Baseline ≥168 maintained (well exceeded). |
| PlayMode   |     7 |    7 |    0 | All green (PerfStressFpsTest now satisfied by scaffold-attached FpsSampler). |

## Deferred / follow-ups
- 11 pre-existing EditMode failures should be triaged by gameplay-engineer (cross-plane bug + AutoAttack + Save bak2 + ProjectileMath). Tracked under ADR-0019.
- SceneFlow currently transitions Boot → Run directly (skips MainMenu/Loadout per crash-fix scope). A later wave should add a real MainMenu landing screen.

## Next gate
Run `bundle exec fastlane beta` from `games/brave-bunny/tools/ci/fastlane/` to produce TestFlight build #3 (0.1.0(3)). Exact command:
```
cd /Users/omeryasironal/Projects/studio/games/brave-bunny/tools/ci/fastlane && bundle exec fastlane beta
```
