# Hand-off notes

Every agent writes a hand-off note here at session end. Filename pattern: `<agent>-<YYYYMMDD-HHMMSS>.md`.

Max 50 lines. Must include: what's done, what's blocked, what the next agent needs to read first.
