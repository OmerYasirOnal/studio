# qa-engineer — re-enable BRAVE_FUTURE_API-guarded tests (Path B)

**When:** 2026-05-12 ~13:00 local
**ADR:** 0015 (test/production API drift) — resolution criteria met (Path B)
**Domain:** `unity/Assets/_Brave/Code/Tests/EditMode/Gameplay/` only

## What I changed

Both previously dead-guarded test files now compile against the *current*
production API (Path B) — no production code touched.

| File | Tests before | Tests after | Notes |
|---|---|---|---|
| `WaveDefinitionTests.cs` | 0 (entire body under `#if BRAVE_FUTURE_API`) | **10** | Pivoted to `WaveSpawnEntry[] events` / `triggerMinute` shape. |
| `MechanicRegistryTests.cs` | 0 (entire body under `#if BRAVE_FUTURE_API`) | **12** | Private-reflection reset; asserts all 8 production `[BraveRegister]` tokens scan. |

## New test names (one-line assertion each)

### WaveDefinitionTests (10)
- `WaveDefinition_NEntries_ExposesNEntries` — `events.Length` round-trips.
- `WaveDefinition_TotalSpawnCount_EqualsSumOfEntries` — sum across entries.
- `WaveDefinition_FirstSpawnTime_IsMinOfTriggerMinutes` — minute-zero lower bound.
- `WaveDefinition_LastSpawnTime_IsMaxOfTriggerMinutes` — boss-minute upper bound.
- `WaveDefinition_AllTriggerMinutes_NonNegative` — no negative trigger times.
- `WaveDefinition_Events_AreSortedByTriggerMinute` — locks `OnValidate` contract.
- `WaveDefinition_BiomeSlug_RoundTrips` — slug field survives assignment.
- `WaveDefinition_SpawnEntry_FieldsRoundTrip` — every struct field round-trips.
- `WaveDefinition_Empty_IsValid_ZeroEntries` — empty array is a valid edge case.
- `WaveDefinition_DefaultInstance_EventsNeverNull` — default SO has `Array.Empty<>`, not null.
- `WaveDefinition_BossEventBeforeRunEnd_DetectablePerType` — Boss event detectable in events.

### MechanicRegistryTests (12)
- `Registry_ScanAssemblies_RegistersAllExpectedTokens` — all 8 tokens resolve after scan.
- `Registry_All_ContainsAtLeastExpectedProductionCount` — floor of 8 enforced.
- `Registry_All_ContainsEveryExpectedToken` — `All.Keys` superset check.
- `Registry_Resolve_KnownToken_ReturnsSignatureMechanicType` — type derives from base.
- `Registry_Resolve_UnknownToken_Throws` — `KeyNotFoundException`.
- `Registry_TryResolve_UnknownToken_ReturnsFalseAndNull` — non-throwing miss path.
- `Registry_TryResolve_KnownToken_ReturnsTrueAndType` — non-throwing hit path.
- `Registry_LookupIsCaseSensitive` — `HOP-DODGE` misses, `hop-dodge` hits.
- `Registry_Construct_KnownToken_ReturnsInstance` — instance round-trips `TypeName`.
- `Registry_Construct_UnknownToken_Throws` — symmetric with Resolve.
- `Registry_ResetThenScan_RebuildsTable` — proves reset+rescan == original.
- `Registry_ScanAssemblies_IsIdempotent` — second call no-ops.

## Production-API gaps surfaced (NOT bridged here)

- `WaveDefinition` still lacks `durationSeconds` and `maxConcurrentEnemies`.
  Once gameplay-engineer adds them, the rolling-window concurrency cap test
  (originally in the disabled file) can be reintroduced — flagged for follow-up.
- `MechanicRegistry.ResetForTests()` exists but is `internal`; tests use
  `BindingFlags.NonPublic` reflection over `_typesByName` / `_initialised`.
  If/when systems-engineer adds `InternalsVisibleTo("Brave.Tests.EditMode")`,
  the reflection helper can collapse to a direct call.

## Verify

- `grep -r "BRAVE_FUTURE_API" unity/Assets/` returns **0 hits** (verified).
- Both files compile against current public surface (read line-by-line).
- No production / other-agent files modified.

## How to run

Unity batch EditMode runner (per Phase-5 cadence):
`./core/scripts/verify-game.sh --game brave-bunny` or the existing Unity
`-runTests -testPlatform EditMode` invocation from `tools/ci/`.
