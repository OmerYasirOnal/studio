# Hand-off — art-director (Wave 5)

**Date:** 2026-05-12 19:54:21
**Agent:** art-director
**Action:** App Store screenshot specification (Brave Bunny: Survivors)

## Deliverables

- **Spec:** `games/brave-bunny/docs/marketing/screenshot-spec.md` (new file, new directory)
- **Locale keys:** `games/brave-bunny/unity/Assets/_Brave/Localization/screenshot-keys.json` (EN filled; TR / tl-PH / id null pending narrative-designer)

## Five chosen headlines (top pick per bank)

1. **Hop. Swarm. Survive.** — sub: "An 8-minute roguelite with a smile on every frame."
2. **One thumb. Big builds.** — sub: "Auto-attack, evolve, and watch a build come alive."
3. **Eight heroes. Eight feels.** — sub: "Tortoise tanks. Fox crits. Owl scales. Pick your style."
4. **Five worlds, one carrot quest.** — sub: "Meadow, beach, forest, cavern, snow — each plays different."
5. **Every level, a tiny puzzle.** — sub: "Three offers, six builds, infinite runs."

All copy passes tone bible (no banned vocab, ≤ 18 words/sentence, no emoji). All use ADR-0016 product name where the product name appears in-frame.

## iPad scope finding

**iPad is NOT in launch scope.** `GAME.md :: platforms = [ios, android]`; no iPad device listed in `target_devices` (only `iphone-12` and `iphone-se-3`). Spec documents iPhone classes only (6.7" / 6.5" / 5.5"). iPad recipe is sketched informationally for a post-launch follow-up but is **not produced** in this dispatch.

## Device classes covered

- Class A (6.7"/6.9"): 1290×2796 — primary modern
- Class B (6.5"): 1284×2778 — substitute-acceptable, still ship explicit set
- Class C (5.5"): 1242×2208 — required unless build-engineer drops iOS-12 support pre-submit

## Localisation gap

- `en` — defined here, all 10 keys populated
- `tr` — **gap**, narrative-designer follow-up (highest priority — largest soft-launch market)
- `tl-PH`, `id` — **gap**, narrative-designer follow-up; English fallback acceptable per `GAME.md` cut-list item #3 if schedule tightens

## Out-of-scope (per task brief)

- No image assets produced (spec only)
- No code, scene, balance JSON, ADR, or other-agent files touched
- App Store display name ADR-0016 untouched

## One-sentence hand-off to the future capture dispatch

> Next: a gameplay-engineer + asset-curator pairing implements an `Editor/CaptureKit/` deterministic spawn-list + camera-pose runner per spec §4, exports five 1290×2796 raw PNGs to `assets-raw/marketing/screenshots/raw/`, then Canva-MCP composes the headline/subhead overlays per spec §7 across the three device classes.
